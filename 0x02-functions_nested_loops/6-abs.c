#include "main.h"

/**
 *_abs - Computes the value of an integer
 * @t: The integer that is computed
 * Return: Absolute value gotten
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
