#include "main.h"

/**
 * _abs - computes the absoulte value of an integer
 * @t: The integer that is computed
 * Returns: Absolute value gotten
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
